#!/bin/bash

# --- gitmoji git commit hook ---
gitmoji --init


# --- GPG Agent ---
# source $HOME/.dotfiles/lib/gpg.sh

# --- SSH Agent ---
eval $(ssh-agent -s) >/dev/null

# --- Broot Init ---
source $HOME/.config/broot/launcher/bash/br

# --- ASDF Init ---
source $ASDF/asdf.sh;
